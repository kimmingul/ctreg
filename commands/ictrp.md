---
description: WHO ICTRP(집계)에서만 찾는다 — 기본으로 꺼져 있다
argument-hint: "[찾고 싶은 것]"
allowed-tools: Bash
---

`--registry ictrp` 로만 조회한다.

**기본으로 꺼져 있다.** WHO 가 자동 조회를 합의된 파트너에게만 열고 비용을 청구하기
때문이다. 꺼져 있으면 켜는 법과 이유가 함께 나온다 — **사용자에게 그대로 전하고 임의로
켜지 마라.** 합의가 있는지는 사용자만 안다.

켜져 있다면: 약 스무 나라의 등록기관을 모은 **집계 사본**이라 다른 곳으로는 안 보이는
시험이 걸린다. 대신 원본보다 며칠 뒤처지고, 비상업 용도로만 쓸 수 있다.

$ARGUMENTS

## 어떻게 부르나

`ctreg` 가 있으면 그것을, 없으면 `npx -y @kimmingul/ctreg` 를 쓴다.

**축과 값을 추측하지 마라.** 이 레지스트리가 무엇으로 검색되고 어떤 값을 받는지는
`ctreg registries` 가 말하고, 커맨드 표면은 `ctreg --help` 가 말한다. 둘 다 네트워크
비용이 거의 없다 — 틀린 플래그로 한 번 부딪히는 것보다 싸다.

## 답하기 전에

- **종료 코드로 갈라라.** 결과 없음과 "그렇게 물어볼 수 없음" 은 다른 답이다. 후자면
  무엇을 대신 물을 수 있는지 사용자에게 말하라.
- **경고를 읽어라.** 잘렸거나 이어받을 수 없으면 그 사실을 답에 담아라.
- 레지스트리 기재사항은 스크리닝을 대체하지 않는다. 적격 판정처럼 쓰지 마라.
