# CTIS 필드 테스트 — 2026-08-30

`CTIS_CAPABILITY` 의 선언을 EMA 공개 API 의 실물에 대조한 결과.
계약 스위트는 스텁으로 돌기 때문에 이런 종류의 어긋남을 원리상 잡지 못한다.

**이 어댑터의 신고는 "조용히 무시된다" 위에 얹혀 있다.** 이 API 는 모르는 검색 키를
버리면서 아무 소리도 내지 않으므로, 어느 키가 진짜로 거르는지를 헛소리 값으로 재서
다섯만 신고하고 나머지는 exit 3 으로 막았다. 그 지형이 바뀌면 **오류가 아니라 조용히
틀린 답** 이 나간다 — 좁혀지지 않은 결과가 좁혀진 것처럼, 또는 열려 있는 축이 막힌 채로.

**「아직도 조용히 무시된다」 줄이 통과하는 것은 좋은 소식이 아니다.** 우리가 키 이름을
잘못 적어도 아무 소리가 나지 않는다는 뜻이기도 하다.

**「상세가 … 를 아직 낸다」 줄들이 이 파일에서 가장 조용한 실패를 지킨다.** `registryId`
는 응답 최상위에서 오므로 그 밑의 깊은 경로가 통째로 옮겨져도 번호 대조는 통과하고,
`get` 은 실패하지 않은 채 **빈 레코드** 를 낸다.

| 무엇 | 판정 | 실측 | 왜 보나 |
| :-- | :-- | :-- | :-- |
| containAll 가 여전히 거른다 (--term) | ✅ 통과 | 211 (기준 12317) | 기준과 같으면 이 키가 조용히 무시되기 시작한 것이고, 좁혀지지 않은 결과가 좁혀진 것처럼 나간다. 0이면 축이 죽었다 |
| title 가 여전히 거른다 (--title) | ✅ 통과 | 134 (기준 12317) | 기준과 같으면 이 키가 조용히 무시되기 시작한 것이고, 좁혀지지 않은 결과가 좁혀진 것처럼 나간다. 0이면 축이 죽었다 |
| medicalCondition 가 여전히 거른다 (--condition) | ✅ 통과 | 178 (기준 12317) | 기준과 같으면 이 키가 조용히 무시되기 시작한 것이고, 좁혀지지 않은 결과가 좁혀진 것처럼 나간다. 0이면 축이 죽었다 |
| sponsor 가 여전히 거른다 (--sponsor) | ✅ 통과 | 164 (기준 12317) | 기준과 같으면 이 키가 조용히 무시되기 시작한 것이고, 좁혀지지 않은 결과가 좁혀진 것처럼 나간다. 0이면 축이 죽었다 |
| msc 가 여전히 거른다 (--location) | ✅ 통과 | 5004 (기준 12317) | 기준과 같으면 이 키가 조용히 무시되기 시작한 것이고, 좁혀지지 않은 결과가 좁혀진 것처럼 나간다. 0이면 축이 죽었다 |
| zzNoSuchKey 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| trialPhase 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| ctStatus 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| ageGroup 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| country 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| product 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| therapeuticArea 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| ctNumber 가 아직도 조용히 무시된다 | ✅ 통과 | 12317 (기준 12317) | 건수가 달라지면 우리가 못 한다고 신고해 둔 축이 실제로는 열려 있다는 뜻이다 — 근거 없이 exit 3 을 주고 있다 |
| msc 40 가 아직 Austria 다 | ✅ 통과 | 1151건 중 20건 확인, 어긋남 0 | 코드가 어긋나면 오류가 아니라 다른 나라의 결과가 나온다. 표가 밀리면 사용자는 그것을 알 방법이 없다 |
| msc 250 가 아직 France 다 | ✅ 통과 | 4637건 중 20건 확인, 어긋남 0 | 코드가 어긋나면 오류가 아니라 다른 나라의 결과가 나온다. 표가 밀리면 사용자는 그것을 알 방법이 없다 |
| msc 276 가 아직 Germany 다 | ✅ 통과 | 4066건 중 20건 확인, 어긋남 0 | 코드가 어긋나면 오류가 아니라 다른 나라의 결과가 나온다. 표가 밀리면 사용자는 그것을 알 방법이 없다 |
| msc 380 가 아직 Italy 다 | ✅ 통과 | 3792건 중 20건 확인, 어긋남 0 | 코드가 어긋나면 오류가 아니라 다른 나라의 결과가 나온다. 표가 밀리면 사용자는 그것을 알 방법이 없다 |
| msc 724 가 아직 Spain 다 | ✅ 통과 | 5004건 중 20건 확인, 어긋남 0 | 코드가 어긋나면 오류가 아니라 다른 나라의 결과가 나온다. 표가 밀리면 사용자는 그것을 알 방법이 없다 |
| msc 752 가 아직 Sweden 다 | ✅ 통과 | 1164건 중 20건 확인, 어긋남 0 | 코드가 어긋나면 오류가 아니라 다른 나라의 결과가 나온다. 표가 밀리면 사용자는 그것을 알 방법이 없다 |
| msc 에 'Spain' 를 보내면 아직 0건이다 | ✅ 통과 | 0 | 이 0건이 나라 이름 가드의 근거다. 이름을 받기 시작했다면 가드가 쓸데없이 막고 있는 것이고, 기준과 같아졌다면 필터가 통째로 증발한 것이다 |
| msc 에 'ES' 를 보내면 아직 0건이다 | ✅ 통과 | 0 | 이 0건이 나라 이름 가드의 근거다. 이름을 받기 시작했다면 가드가 쓸데없이 막고 있는 것이고, 기준과 같아졌다면 필터가 통째로 증발한 것이다 |
| 상세가 제목(trialDetails.clinicalTrialIdentifiers) 를 아직 낸다 | ✅ 통과 | Clinical trial of MK-7648A in adults and children with skin  | 이 경로가 옮겨지면 get 은 오류가 아니라 그 자리가 빈 레코드를 낸다 — 번호 대조는 최상위 ctNumber 로 통과하기 때문이다 |
| 상세가 문자열 상태(ctStatus) 를 아직 낸다 | ✅ 통과 | Not authorised | 이 경로가 옮겨지면 get 은 오류가 아니라 그 자리가 빈 레코드를 낸다 — 번호 대조는 최상위 ctNumber 로 통과하기 때문이다 |
| 상세가 질환(medicalConditions[]) 를 아직 낸다 | ✅ 통과 | High-risk Resected Melanoma | 이 경로가 옮겨지면 get 은 오류가 아니라 그 자리가 빈 레코드를 낸다 — 번호 대조는 최상위 ctNumber 로 통과하기 때문이다 |
| 상세가 의뢰기관(sponsors[].publicContacts[].organisation.name) 를 아직 낸다 | ✅ 통과 | Merck Sharp & Dohme LLC | 이 경로가 옮겨지면 get 은 오류가 아니라 그 자리가 빈 레코드를 낸다 — 번호 대조는 최상위 ctNumber 로 통과하기 때문이다 |
| 상세가 참여국(memberStatesConcerned[].mscName) 를 아직 낸다 | ✅ 통과 | 9 | 이 경로가 옮겨지면 get 은 오류가 아니라 그 자리가 빈 레코드를 낸다 — 번호 대조는 최상위 ctNumber 로 통과하기 때문이다 |
| 상세가 대상자 수(rowSubjectCount) 를 아직 낸다 | ✅ 통과 | 1296 | 이 경로가 옮겨지면 get 은 오류가 아니라 그 자리가 빈 레코드를 낸다 — 번호 대조는 최상위 ctNumber 로 통과하기 때문이다 |
| 레코드마다 출처 표시가 실린다 | ✅ 통과 | Source: European Medicines Agency (EMA) — Clinical Trials Information System (CTIS) | EMA 법적 고지가 each copy 에 요구하는 것이다 — 빠지면 결함이 아니라 이용 조건 위반이다 |
| 없는 번호는 아직 빈 응답이다 | ✅ 통과 | HTTP 200, 키 0개 | 빈 응답이라 번호 대조에서 걸려 not_found 가 된다. 404 나 오류로 바뀌면 없는 시험이 레지스트리 장애로 보고된다 |
| 없는 번호가 오류가 아니라 not_found 다 | ✅ 통과 | 레코드 0건, 경고 not_found | "그런 시험이 없다" 와 "레지스트리가 고장났다" 가 같은 출력이 되면 안 된다 |
| maxPageSize 50 를 요청하면 그만큼 온다 | ✅ 통과 | 50 를 요청해 50건 (전체 211건) | 적게 오면 페이지 계산이 어긋나 뒷쪽을 건너뛴다 — 조용히 빠진 시험이 생긴다 |
| 상태 코드 11 이 아직 'Not authorised' 다 | ⚠️ 불확정 | 이 표본 쪽에 나오지 않았다 | 검사가 돌지 않았다는 뜻이지 통과했다는 뜻이 아니다. 드문 코드라 다른 질의에서 다시 재야 한다 |
| 상태 코드 8 이 아직 'Ended' 다 | ✅ 통과 | 2023-508067-70-00 → 'Ended' | 이 둘만 공통 어휘로 접는다. 뜻이 바뀌면 끝난 시험을 다른 상태로 신고하게 된다 |
| 접지 못한 코드들이 아직 갈리지 않는다 | ✅ 통과 | 2→'Authorised', 3→'Authorised', 4→'Authorised', 5→'Authorised' | 전부 'Authorised' 이면 unknown 으로 둔 판단이 여전히 옳다. 갈리기 시작했다면 실패가 아니라 표를 넓힐 기회다 |
| 자유 텍스트에 번호를 넣으면 그 한 건이 나온다 | ✅ 통과 | 1건: 2022-501417-31-00 | ctNumber 로 거는 자리가 없어 get 이 이 우회에 얹혀 있다. 여러 건이 오면 대조가 첫 건을 버리게 되고, 0건이면 get 이 통째로 죽는다 |

통과 34 / 실패 0 / 불확정 1
