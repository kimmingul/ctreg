# 에이전트 필드 테스트 전사 — 2026-08-22

`docs/agent-field-test-2026-08-22.md` 의 부록이다. 그 문서의 모든 판정은 여기 있는
전사에서 나왔고, 모든 숫자는 `ground-truth-*.md` 와 대조됐다.

- `s1.md`~`s6.md` — 본 실행 여섯. 각각 실행한 명령과 출력(축어), 최종 답,
  헤맨 지점, 확신이 없는 지점.
- `s3-haiku.md`·`s4-haiku.md` — 다른 모델 계열로 재실행한 둘.
- `ground-truth-s1.md`·`ground-truth-s2-s6.md` — 컨트롤러가 CLI 를 직접 돌려 만든 정답표.

**왜 커밋하는가.** 스펙 §7 은 전사를 부록이라 부른다. 사라지는 부록은 부록이 아니다.
이 문서들의 주장은 실측이고, 실측은 나중에 확인할 수 있어야 한다. 원본은 gitignore 된
`.superpowers/fieldtest/` 에 있었고 그것은 작업 중 스크래치라 지워진다.

**날짜에 유의.** ClinicalTrials.gov 는 매일 갱신된다. 여기 적힌 숫자는 2026-08-22 의 값이고,
나중에 재측정하면 달라진다. 달라졌다는 것이 결함의 증거는 아니다.
